# configuração local do Claude Code — não vai para o repositório
.claude/settings.local.json

# gerados pelo build — não precisam ir para o repositório
# (dashboard-completo.html na raiz é a exceção: é servido pelo site)
dist/
_site/
__pycache__/
